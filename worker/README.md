# Worker deployment notes

Keep the existing SETTINGS_KV binding. Optionally add a D1 binding named PORTFOLIO_DB using `schema.sql`.

Set secrets: TELEGRAM_BOT_TOKEN, GITHUB_TOKEN, ADMIN_SECRET, TELEGRAM_WEBHOOK_SECRET.
