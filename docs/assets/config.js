window.ARZPULSE_CONFIG = Object.freeze({
  // Leave blank when using the configure button in Portfolio.
  WORKER_URL: '',
  PROFILE_API_TIMEOUT_MS: 12000,
  DATA_FETCH_TIMEOUT_MS: 9000,
  DATA_FETCH_RETRIES: 2
});
